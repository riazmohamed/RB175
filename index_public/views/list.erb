<ul>
  <% @files.each do |file| %>
    <li><%= file %></li>
  <% end %>
</ul>

<% if params[:sort] == "desc" %>
  <a href="/">Sort acsending</a>
<% else %>
  <a href="/?sort=desc">Sort decsending</a>
<% end %>
